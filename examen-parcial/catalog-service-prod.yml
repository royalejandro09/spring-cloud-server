server:
  port: 8085

management:
    endpoints:
        web:
            exposure:
                include: info,health

info:
  app:
    name: catalog-service
    description: Catalog service
    version: 0.0.1
